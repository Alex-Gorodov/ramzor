export type User = {
  firstName: string;
  secondName: string;
  id: number;
}

export type UserAuthData = {
  id: number;
  firstName: string;
  secondName: string;
};

export type AuthData = {
  id: number;
}